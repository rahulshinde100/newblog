<html>
<head>
  </head>
  <body>

<center>

<%= form_for(@tag) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_title %><br>
    <%= f.text_field :tag_title , :placeholder=>'Enter the Tag' , required: true %>
  </div><br><br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<br><br>
<%= link_to 'Home', posts_path %><br><br>
</center>
</body>
</html>